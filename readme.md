# gRPC Demo

A simple demo project showcasing how to integrate gRPC with Spring Boot.

## Installation
Build the shared proto module:

```bash
cd inventory-proro
mvn clean install -U 
```

## Usage

Use the provided Postman Collection to test the gRPC endpoints.

